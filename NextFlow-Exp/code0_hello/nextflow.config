process {
    executor = 'slurm'
    queue = 'default'     // adjust to your cluster queue/partition
    clusterOptions = '--ntasks=1 --partition=barnard --account=p_nanoparticle --time=00:05:00 --cpus-per-task=4'
}
